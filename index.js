const program = require('commander')

const version = require('./package.json').version;
program
    .description('Generate files for Docusaurus-powered documentation from outline files')
    .version(versio

